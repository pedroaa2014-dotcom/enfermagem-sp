import { BarChart3 } from 'lucide-react'; import { ComparisonScores,UNKNOWN } from '@/data/universities'; import { SectionTitle } from './BenefitsSection';
const labels:[keyof ComparisonScores,string][]=[['academic','Qualidade acadêmica'],['structure','Estrutura'],['value','Custo-benefício'],['location','Localização'],['practice','Experiência prática'],['career','Mercado de trabalho']];
export function ScoreBars({scores,overallScore,costBenefitLabel}:{scores:ComparisonScores;overallScore?:number;costBenefitLabel?:string}){return <section className="detail-section"><SectionTitle icon={BarChart3} title="Índice Enfermagem SP"/><div className="index-summary"><strong>{overallScore?.toFixed(1)??UNKNOWN}</strong><span>Nota geral comparativa</span>{costBenefitLabel&&<em>Custo-benefício: {costBenefitLabel}</em>}</div><div className="scores-grid">{labels.map(([key,label])=><div className="score-item" key={key}><div><span>{label}</span><strong>{scores[key]!==undefined?`${scores[key]}/10`:UNKNOWN}</strong></div><div className="score-track"><i style={{width:`${(scores[key]||0)*10}%`}}/></div></div>)}</div><p className="score-disclaimer">Esta é uma avaliação comparativa do Enfermagem SP baseada em indicadores acadêmicos, estrutura, experiência prática, custo e localização. Não representa uma nota oficial do MEC.</p></section>}
