import { ExternalLink,Link2 } from 'lucide-react'; import { Source } from '@/data/universities'; import { PendingBlock,SectionTitle } from './BenefitsSection';
export function SourcesSection({sources,lastUpdated}:{sources:Source[];lastUpdated:string}){return <section className="detail-section sources-section"><SectionTitle icon={Link2} title="Fontes e última atualização" aside={`Consultado em ${lastUpdated}`}/>{sources.length?<div className="sources-list">{sources.map(s=><a key={`${s.url}-${s.type}`} href={s.url} target="_blank" rel="noreferrer"><span><small>{s.type} · {s.accessedAt}</small><strong>{s.label}</strong></span><ExternalLink size={17}/></a>)}</div>:<PendingBlock text="Fontes a confirmar"/>}<p className="data-caution">Dados acadêmicos e valores podem sofrer alterações. Consulte sempre a instituição antes de realizar a matrícula.</p></section>}
