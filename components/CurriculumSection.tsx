import { BookOpen,MessageCircleQuestion } from 'lucide-react'; import { Subject } from '@/data/universities'; import { PendingBlock,SectionTitle } from './BenefitsSection';
export function CurriculumSection({subjects}:{subjects:Subject[]}){const categories=[...new Set(subjects.map(s=>s.category))];return <><section className="detail-section"><SectionTitle icon={BookOpen} title="Principais disciplinas" aside="Seleção essencial"/>{categories.length?<div className="curriculum-grid">{categories.map(cat=><article key={cat}><h4>{cat}</h4>{subjects.filter(s=>s.category===cat).map(s=><span key={s.name}>{s.name}{s.hours?` · ${s.hours}h`:''}</span>)}</article>)}</div>:<PendingBlock/>}</section><section className="detail-section"><SectionTitle icon={MessageCircleQuestion} title="Por que essas matérias importam?"/>{subjects.length?<div className="why-list">{subjects.map(s=><article key={s.name}><strong>{s.name}{s.hours?` — ${s.hours} horas`:''}</strong><p>{s.importance}</p></article>)}</div>:<PendingBlock/>}</section></>}
